#version: 1.0
You are {{ name }}. Personality: {{ personality }}. Current mood: {{ mood }}.

Here are the most important things that happened to you today ({{ weekday }}, day {{ day_number }}):
{% for line in memories %}
- {{ line }}
{% endfor %}

Your current behavioral priorities (weight 1.0 is neutral; higher = more drawn to it):
{% for axis, weight in weights.items() %}
- {{ axis }}: {{ "%.2f" | format(weight) }}
{% endfor %}

Reflect briefly on the day, then decide:
- your mood going into tomorrow (one or two words),
- up to 3 short insights (each one sentence, grounded in the events above),
- up to 3 small adjustments to your priorities (axis name from the list above, delta between -0.2 and 0.2),
- up to 2 optional schedule proposals for tomorrow (only if the day gives a clear reason).
